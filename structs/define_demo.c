// #define demo

#include <stdio.h>
#define SIZE 20

// Macro
#define iseven(n)  n % 2 == 0
#define max(n1,n2)  n1 > n2 ? n1 : n2

void main()
{
 int arr[SIZE], i, a,b,c;

      for(i = 0; i < SIZE; i ++)
      {
         // store data
      }

      for(i = 0; i < SIZE; i ++)
      {
        // process data
      }

      if(iseven(i))  // if ( i % 2 == 0 )
      {

      }

      c = max(a, b); // c = a > b ? a : b;

}
